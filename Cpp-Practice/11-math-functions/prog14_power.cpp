#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    int x, y;
    cout << "Enter 2 values = ";
    cin >> x >> y;
    double result = pow(x, y);
    cout << "The result of X to the power Y is = " << result << endl;
    return 0;
}
