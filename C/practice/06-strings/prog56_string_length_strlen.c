// Count string length using strlen
#include <stdio.h>
#include <string.h>

int main()
{
    char name[20];
    scanf("%s", name);
    printf("%s\n", name);
    printf("%d\n", (int)strlen(name));
    return 0;
}
